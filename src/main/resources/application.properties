# Server Configuration
server.port=10000
# server.servlet.context-path=/api

# Application Info
spring.application.name=EcoBazaarX Backend
spring.application.version=1.0.0

# MySQL Database Configuration (Render Production)
spring.datasource.url=jdbc:mysql://centerbeam.proxy.rlwy.net:36676/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&connectTimeout=60000&socketTimeout=60000
spring.datasource.username=root
spring.datasource.password=cOkdTzTVvUIwNKaGinyHGFGipGhBqYWr
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# H2 Database (for testing only)
spring.h2.console.enabled=false
spring.h2.console.path=/h2-console

# Database Configuration - Using MySQL

# Security Configuration
jwt.secret=ecobazaarX2024SecretKeyForJWTTokenGeneration
jwt.expiration=86400000

# Logging Configuration
logging.level.com.ecobazaar=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:5173
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Cache Configuration
spring.cache.type=caffeine
spring.cache.cache-names=users,products,settings,ecoChallenges,userChallenges
spring.cache.caffeine.spec=maximumSize=500,expireAfterWrite=600s



